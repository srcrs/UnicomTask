version: "1"
rules: # Array of rules
    - base: main # Required. Target branch
      upstream: srcrs:main # Required. Must be in the same fork network.
      mergeMethod: mergeMethod: merge # Optional, one of [none, merge, squash, rebase, hardreset], Default: hardreset.
      mergeUnstable: true # Optional, merge pull request even when the mergeable_state is not clean. Default: true
